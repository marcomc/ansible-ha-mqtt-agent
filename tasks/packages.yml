---
- name: Install ha-mqtt-agent runtime packages
  ansible.builtin.apt:
    name: "{{ ha_mqtt_agent_apt_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
  become: true
